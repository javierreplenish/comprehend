ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
PORT=8787
CLIENT_ORIGIN=http://localhost:5173
SESSION_SECRET=change-this-to-a-long-random-string
DB_PATH=./data.db

# Admin — your email address, only this account sees the Admin dashboard
ADMIN_EMAIL=smithjavier500@gmail.com

# Stripe — get these from https://dashboard.stripe.com
# Secret key: Dashboard → Developers → API keys
# Price ID: Dashboard → Products → create a product → copy the price ID (starts with price_)
# Webhook secret: Dashboard → Developers → Webhooks → add endpoint → copy signing secret
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
